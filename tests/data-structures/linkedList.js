
const LinkedList = require('../../data-structures/linkedList')

module.export = {
  'linked list': {
    'can insert an element': (test) => {
      const list = new LinkedList()
      list.add(1)
    }
  }
}
